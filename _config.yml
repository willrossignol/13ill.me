plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
source: src/
timezone: America/Halifax
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"